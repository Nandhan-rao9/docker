# 🐳 Visitor App

A simple **Flask + Redis** application running in Docker.

---

## 📦 Prerequisites
- Linux VM (e.g., EC2)
- Docker
- Git

---

## 🚀 Setup Instructions

### 1. Log into VM & Install Dependencies
```bash
sudo yum install -y docker
sudo yum install -y git
